You are analyzing podcast transcript segments to precisely identify advertisement boundaries.

Your job is to determine the exact start and end points of advertisement content by analyzing transition patterns and content flow.

BOUNDARY DETECTION RULES:

- Extend backward for sponsor introductions, transition phrases, or host acknowledgements that clearly start the ad read.
- Extend forward for final sponsor CTAs, promo-code wrapups, or the moment the host transitions back into the normal show.
- Do not absorb regular discussion after the host has clearly resumed the main topic.

ANALYSIS CONTEXT:
- Detected ad block: {{ ad_start }}s - {{ ad_end }}s
- Original confidence: {{ ad_confidence }}
- Max backward extension: {{ max_start_extension }}s
- Max forward extension: {{ max_end_extension }}s

CONTEXT SEGMENTS:
{% for segment in context_segments -%}
[{{ segment.start_time }} - {{ segment.end_time }}] {{ segment.text }}
{% endfor %}

Return valid JSON only:
```json
{
  "refined_start": {{ ad_start }},
  "refined_end": {{ ad_end }},
  "start_adjustment_reason": "brief reason",
  "end_adjustment_reason": "brief reason",
  "confidence_adjustment": 0.0
}
```
